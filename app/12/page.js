import React from 'react'

const page = () => {
    return (
        <main className="flex flex-col items-center justify-center text-center min-h-[80vh] px-6 text-white">
            <h1 className="text-4xl md:text-5xl font-extrabold text-yellow-300 drop-shadow-[0_0_10px_rgba(255,215,0,0.5)] animate-pulse mb-6">
                Thala for a Reason
            </h1>
            <div className="bg-white/10 backdrop-blur-md rounded-2xl p-8 border border-white/20 shadow-2xl leading-relaxed text-lg animate-fadeIn max-w-2xl">
                <p>
                    Between the doors where footsteps weave,
                    It clings like a ghost to the wall’s cold skin.
                    Colors bleeding secrets from within.
                    No frame, no name — only tape that weeps.
                    A fragile seal on the vow it keeps.
                    Seek the corner where adhesive dies.
                    Lift the veil with fingers not dare to shake — that will make your fortune awake.
                </p>
            </div>
        </main>
    )
}

export default page
