import React from 'react'

const page = () => {
    return (
        <main className="flex flex-col items-center justify-center text-center min-h-[80vh] px-6 text-white">

            <div className="bg-white/10 backdrop-blur-md rounded-2xl p-8 border border-white/20 shadow-2xl leading-relaxed text-lg animate-fadeIn max-w-2xl">
                <p>
                    Where circuits whisper in electric verse,
                    And algorithms dance through code’s reverse.
                    A canvas hangs in silicon’s embrace — not parchment, but a portal to cyberscape.
                    Pinned by pixels, not by hand, you’ll see.
                    Point your lens where announcements blend — the clue downloads at the message end.
                </p>
            </div>
        </main>
    )
}

export default page
